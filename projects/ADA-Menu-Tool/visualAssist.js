	//Visual Focus Control 
		
		document.addEventListener("DOMContentLoaded", () => {
  const visualFocusSwitch = document.getElementById("visual_focus_switch");
  if (!visualFocusSwitch) return;

  // (rest of your existing logic stays the same)
});
